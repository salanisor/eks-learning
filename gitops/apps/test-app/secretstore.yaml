apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: test-app-secret-store
  namespace: test-app
spec:
  provider:
    aws:
      service: SecretsManager
      region: us-east-1
      auth:
        jwt:
          serviceAccountRef:
            name: external-secrets
            namespace: external-secrets
