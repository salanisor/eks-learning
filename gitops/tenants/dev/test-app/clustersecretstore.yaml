apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: test-app-secrets
spec:
  provider:
    aws:
      service: SecretsManager
      region: us-east-1
      role: arn:aws:iam::684177687615:role/eks-learning-eso-test-app
